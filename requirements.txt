# Taylor's Transcriber — Python dependencies
#
# YOU DO NOT NEED TO RUN pip YOURSELF.
#
# Launch the app with ./run_subtitler.sh (macOS/Linux), run_subtitler.bat
# (Windows), or by double-clicking "Taylor's Transcriber.command" on macOS.
# On first run the app creates its own virtual environment at .venv inside this
# folder and installs the two packages below into it. Nothing is installed into
# your system Python — which also avoids the PEP 668
# "externally-managed-environment" refusal you get from Homebrew and Debian
# Python when installing globally.
#
# This file is kept as the canonical record of the base dependencies; the
# authoritative list the bootstrapper uses is BASE_REQUIREMENTS in bootstrap.py.

pywebview>=4.4
huggingface-hub>=0.23.0


# ============================================================================
# Optional extras — install from the Settings panel, not from here
# ============================================================================
#
# Speech runtimes and the word-timing aligner are installed on a button click in
# Settings, into the app's own environment. They are listed here for reference
# only; there is no need to uncomment anything.
#
# Apple Silicon (use the GPU):
#   mlx-whisper>=0.4.0        Whisper family on the Apple GPU
#   parakeet-mlx>=0.3.0       Fastest accurate English on Apple Silicon
#   mlx-qwen3-asr>=0.1.0      Strongest multilingual accuracy
#
# NVIDIA GPU or generic CPU:
#   faster-whisper>=1.0.0     No Metal backend, so CPU-only on a Mac
#
# Any Hugging Face checkpoint (Cohere Transcribe):
#   transformers>=4.40.0
#   torch>=2.2.0
#
# Granite Speech (its processor classes need a newer transformers release):
#   transformers>=4.52.1
#   torch>=2.2.0
#
# Word-timing forced aligner (recommended for captions):
#   torch>=2.2.0
#   torchaudio>=2.2.0
#
# Speaker separation (who is talking, so no caption holds two people):
#   speechbrain>=1.0.0
#   torch>=2.2.0
#   torchaudio>=2.2.0


# ============================================================================
# ProRes export
# ============================================================================
#
# ffmpeg is a system binary, not a Python package:
#   macOS          brew install ffmpeg
#   Debian/Ubuntu  sudo apt install ffmpeg
#   Windows        https://www.gyan.dev/ffmpeg/builds/
