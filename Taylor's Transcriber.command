#!/bin/bash
# Double-click this file in Finder to launch Taylor's Transcriber.
# First run sets up a private virtual environment in your Application Support
# folder. If macOS blocks this file, right-click it and choose Open, or run
# ./run_subtitler.sh from Terminal instead.
cd "$( dirname "${BASH_SOURCE[0]}" )"
./run_subtitler.sh
