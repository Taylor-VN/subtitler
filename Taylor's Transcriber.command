#!/bin/bash
# Double-click this file in Finder to launch Taylor's Transcriber.
# First run sets up a private virtual environment inside the app folder.
cd "$( dirname "${BASH_SOURCE[0]}" )"
./run_subtitler.sh
